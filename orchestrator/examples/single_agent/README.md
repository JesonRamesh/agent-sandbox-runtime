# Single-Agent Example

This is the smallest orchestrator scenario in the repo: one scenario,
one manifest, one Python script, no daemon required.

Run it from the `orchestrator/` directory with:

```bash
python -m orchestrator run -f examples/single_agent/scenario.yaml
```

Use it as the "hello world" path before moving on to `two_agent/` or
the prompt-injection demo.
