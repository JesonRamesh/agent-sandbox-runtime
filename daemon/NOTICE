Agent Sandbox Runtime — Sandbox Daemon
Copyright 2026 The Agent Sandbox Authors.

This product is licensed under the Apache License, Version 2.0. See LICENSE.

The eBPF C programs that this daemon loads at runtime live in the sibling
`bpf/` directory of this repository (authored on the `Mehul` branch) and
are licensed independently — see their headers and `bpf/common.h`.

A read-only mirror of the C-side header is vendored at
`daemon/bpf/common.h.reference` for review-time validation of the Go
struct mirrors. The original file is GPL-2.0; the mirror is unmodified.
